---
description: Create distinctive, production-grade frontend interfaces
---

Build UI components, pages, or applications with React frameworks, Tailwind CSS v4, shadcn/ui, and Motion.

First, invoke the skill tool to load the frontend-design skill:

```
skill({ name: 'frontend-design' })
```

Then follow the skill instructions to create the interface.

<user-request>
$ARGUMENTS
</user-request>
