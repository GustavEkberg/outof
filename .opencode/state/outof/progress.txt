# Progress Log

PRD: outof
Started: 2026-03-02

## Codebase Patterns

- Effect-TS services use `Effect.Service<T>()('@app/Name', { effect: ... })` with `static layer = this.Default; static Live = this.layer.pipe(Layer.provide(ConfigLive))`
- Config via `Config.string()` / `Config.redacted()`, exception: `process.env.TELEGRAM_WEBHOOK_SECRET` in route
- Domain queries in `lib/core/<domain>/queries.ts`, return `Effect.Effect<T, E, Db>`
- Errors as `Data.TaggedError` in `lib/core/errors/` and `lib/services/*/errors.ts`
- All files kebab-case, imports via `@/` alias, no barrel files

---

## Task - setup-1

- Verified existing scaffold: Next.js 16, Effect-TS, Drizzle, Turso deps all present in package.json
- tsconfig has strict mode + `@/*` path alias
- .env.example exists with required env vars
- pnpm install succeeds, pnpm tsc passes, pnpm lint clean
- Added `.opencode/` to `.prettierignore` (was causing false failures)
- Files changed: `.prettierignore`
- **Learnings:** Codebase was pre-scaffolded with full implementation across all layers. All 13 PRD tasks appear code-complete; just need verification passes.

## Task - db-1

- Verified Drizzle schema: `item` (id, chatId, title, userName, createdAt), `shoppingList` (id, chatId, name, createdAt), `shoppingListItem` (id, shoppingListId, itemId, title, userName)
- All tables have CUID2 primary keys, appropriate indexes and foreign keys
- `drizzle.config.ts` configured with `dialect: 'turso'`, reads env vars
- `pnpm db:push` script wired to `drizzle-kit push`
- pnpm tsc passes, pnpm lint clean
- Files changed: none (already correct)
- **Learnings:** Schema uses `$defaultFn(() => createId())` for CUID2 IDs; `integer('col', { mode: 'timestamp' })` for SQLite timestamp columns; `uniqueIndex` for composite unique constraints

## Task - service-db

- Verified Db service: `Effect.Service<Db>()('@app/Db', ...)` with Drizzle client over libsql
- Internal `TursoConfig` tag reads `TURSO_DATABASE_URL` (Config.string) and `TURSO_AUTH_TOKEN` (Config.redacted, optional)
- `Db.Live` provides `TursoConfigLive` layer; exports `Database` type alias
- `lib/layers.ts` merges `Db.Live` + `Telegram.Live` into `AppLayer`
- pnpm tsc passes, pnpm lint clean
- Files changed: none (already correct)
- **Learnings:** `Config.redacted` returns `Redacted<string>`; use `Redacted.value()` to extract. Auth token made optional via `catchAll(() => Effect.succeed(undefined))` for local dev without Turso auth.
